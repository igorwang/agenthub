query GetAgentListByType {
  agent_type(where: { agents: { id: { _is_null: false } } }) {
    id
    name
    agents {
      id
      name
      description
      avatar
    }
  }
}

query AgentByID($id: Int = 10) {
  agent_by_pk(id: $id) {
    id
    name
    description
    avatar
  }
}
